<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing counter</h1>

  <%= render "form", counter: @counter %>

  <%= button_to "Delete", @counter, method: :delete, class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 font-medium", data: { turbo_confirm: "Are you sure you want to delete?" } %>
</div>
