<% content_for :title, "New Counter" %>

<div class="mx-auto md:w-2/3 w-full border border-gray-300 rounded-xl p-8">
  <h1 class="font-bold text-4xl">New counter</h1>

  <%= render "form", counter: @counter %>

  <%= link_to "Cancel", counters_path, class: "button-soft" %>
</div>
